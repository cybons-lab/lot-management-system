/**
 * SmartRead OCR TanStack Query hooks
 *
 * NOTE: このファイルは後方互換性のために残されています。
 * 実装は hooks/ ディレクトリに分割されました。
 *
 * 新しいインポートは直接 ./hooks からインポートしてください:
 * import { useSmartReadConfigs } from "./hooks";
 */

// Re-export all hooks from the new modular structure
export * from "./hooks/index";
