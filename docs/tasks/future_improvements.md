# Future Tasks

## Frontend

- [ ] **Type Safety**: Introduce `openapi-typescript` to automatically generate TypeScript types from the backend OpenAPI schema. This will ensure strict type consistency between the frontend and backend and reduce manual maintenance overhead.
